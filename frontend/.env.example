# Frontend environment variables
# Copy to .env.local for local development

# Optional. Leave unset for relative /api/* (Vite dev server proxies /api to localhost:8000).
# Set only for cross-origin API (e.g. phone testing against LAN IP).
# VITE_API_URL=http://localhost:8000
